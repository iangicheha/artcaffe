{
  "version": 2,
  "builds": [
    {
      "src": "Order/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "build" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/Order/$1" }
  ]
}
